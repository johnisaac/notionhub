var link = "";
link = link + "<li id=link_"+<%=@link.id%> +" class='span-15 link rounded-corners'>"+
			  "<table class='rating span-1' cellpadding='0' cellspacing='0'>"+
			  "<tr><td>"+
			  "<%=escape_javascript(link_to (image_tag 'up.png', options =  { :title => 'You cannot vote for your link', :class=>'left span-1 owner' }), '#')%>"+
			  "</td></tr>"+
			  "<tr><td class='link_rating' style='padding-left:10px;font-weight:bold;'"+<%=@link.rating%>+"</td></tr>"+
			  "<tr><td>"+
			  "<%=escape_javascript( link_to (image_tag 'down.png', options =  { :title => 'You cannot vote for your link', :class=>'left span-1 owner' }), '#')%>"+					
    		  "</td></tr></table>"+
    		  "<span class='link_title span-13'>"+
			  "<%=escape_javascript(link_to truncate(@link.title, :length=>120), @link.URL ) %></span>"+
			  "<span class='link_details span-13'>"+
		      "<%=escape_javascript(link_to truncate(@link.user.name, :length=>15), show_profile_path(:id => @link.user_id), :class => 'left link_owner')%>"+
			  "<span class='time_ago'>"+
			  "less than a minute"+
  			  "</span>"+
			  "<%=escape_javascript(link_to 'Edit','#', :class=>'link_edit')%>"+
			  "<%=escape_javascript(link_to 'Delete','#', :class=>'link_delete')%>";
			  <%if @link.l_comments.count > 0 %>
				link = link + "<a href="+<%=link_path(@link.id)%>+" class='link_comments right'>"+<%=@link.l_comments.count%>+" Comments</a>"
			  <%else%>
   			  	link = link + "<%=escape_javascript(link_to 'Discuss', link_path(@link.id), :class => 'link_comments right')%>";
		      <%end%>
link = link + "</span></li>";

